spring.application.name=backend
server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/navalhex}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:86320590}

spring.flyway.enabled=true
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,https://*.vercel.app}
